import { Routes } from '@angular/router';

export const routes: Routes = [
    //{ path: 'court/:id', component: CourtBookingComponent },
   // { path: 'turf-details', component: TurfDetailComponent }
];

  